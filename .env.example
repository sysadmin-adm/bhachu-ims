# Database — Neon PostgreSQL
DATABASE_URL="postgresql://user:password@host/bhachu_ims?sslmode=require"

# Server
PORT=3000
NODE_ENV=development

# CORS — frontend URL
FRONTEND_URL=http://localhost:5173

# Default org slug (used for MVP single-org seeding)
DEFAULT_ORG_SLUG=bhachu-industries
